<div class="purpose">
  <h2>Need a name for your new server? Why not choose one from the Marvel comic universe.</h2>
  <p>Don't like the one you got? Just <strong>refresh</strong>.</p>
</div> <!--  .purpose -->

<div class="all-characters">
<% if @characters %>
<% @characters.each do |character|  %>
  <div class="single-character">
    <div class="avatar">
      <img class="character-img" src="<%= character['thumbnail']['path'] %>.<%= character['thumbnail']['extension'] %>" alt="<%= character['name'] %>">
    </div> <!--  .avatar -->
    <p>Your next Superhero server could be called <strong><%= character['name'] %></strong></p>

    <div class="hostname-suggestion">
      <p>A machine friendly version would be something like this:</p>
      <pre><%= hostname(character['name']) %></pre>
    </div> <!--  .hostname-suggestion -->

  </div> <!--  .single-character -->
<% end %>

<% else %>
  <div class="limit-reached">

    <p>Oh No! It looks like we have hit the Marvel API limit, try again in a bit.</p>
  </div> <!--  .limit-reached -->
<% end %>

</div> <!--  .all-characters -->